/**
 * Optimization
 *
 * Input dataset :
 * Turn devices+buildings into jobs
 * Engineers into Vehicles
 * Relations : Only if we pin a vehicle to a job
 *
 * Constraints/options :
 * Clustering (On, off, settings...)
 * Objectives (max-load, activities, distance, duration... etc.)
 */
export class Problem {
  constructor() {}
}
